/**
 * Landing theme — kept for backwards-compatible import paths.
 *
 * The canonical design tokens now live in `@/src/shared/lib/theme` so they can
 * be shared between the landing page and the dashboard. This file simply
 * re-exports them.
 */
export * from "@/src/shared/lib/theme";
