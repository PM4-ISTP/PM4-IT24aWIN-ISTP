apiVersion: v1
kind: Secret
metadata:
  name: keycloak-admin-api-client
type: Opaque
stringData:
  # Keycloak confidential client secret for the backend service account client (e.g. clientId: istp-backend)
  # Create a copy of this file (or create the secret in Rancher UI) and set the real value.
  client-secret: ""

